{
  "name": "latest-news-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page for latest news mentions (multi-locale Google News RSS → JSON).",
  "scripts": {
    "fetch:news": "node scripts/fetch-news.js",
    "prerender":  "node scripts/prerender.js"
  },
  "dependencies": {
    "node-fetch": "^2.7.0",
    "rss-parser": "^3.13.0"
  }
}
